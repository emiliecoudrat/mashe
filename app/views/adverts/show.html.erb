<p id="notice"><%= notice %></p>

<div class="container">
     <% content_for(:title) do %>
      <%= "#{@advert.title}" %>
      <% end %>

    <div class="row">
      <div class="col-xs-12 col-sm-12">
        <h1> Share annonce <%= "#{@advert.title}"%> </h1>
        <div>Tags: <%= @advert.category_list.join(", ") %></div>
        <div>Tags: <%= @advert.category_list.join(", ") %></div>
      </div>
    </div>

    <div class="row">
      <div class="col-xs-6 col-sm-4">
        <div class="transaction-type">
          <span> Opportunités : <%= "#{@advert.transaction_type}"%> </span>
          <span> Titre : <%= "#{@advert.title}"%> </span>
        <div class="price_cents">
          <span class="pricetag">
          <span> Prix : <%= "#{@advert.price_cents}"%>€</span>
        </div>
      </div>

      <div class="col-xs-6 col-sm-4">
        <div class="description">
          <span> Description : <%= "#{@advert.description}"%> </span>
        </div>
        <div class="updated_at">
        <span> Mise à jour le : <%= "#{@advert.updated_at}"%> </span>
      </div>
      </div>

      <div class="col-xs-6 col-sm-4">
        <div> <%= image_tag @advertpicture.picture.url(:medium) %> </div>
      </div>


  <div class="row">
    <div class="col-xs-12">
      <div class="created_at">
        <span> Publiée le : <%= "#{@advert.created_at}"%> </span>
      </div>
    </div>
  </div>

  <div class="row">
    <div class="col-xs-2">
      <%= link_to 'Edit', edit_school_advert_path(@school, @advert), class:"btn-dmc" %>
    </div>
    <div class="col-xs-2">
      <%= link_to 'Retour', school_adverts_path(@school) %>
    </div>
  </div>

</div>