<h1 class="text-center"><%= @parent.first_name %> <%= @parent.last_name %></h1>
<div class="container text-center">
  <div class="row">
    <div class="col-xs-12">
      <p><strong>First name:</strong> <%= @parent.first_name %></p>
      <div class="spacer"></div>
      <p><strong>Last name:</strong> <%= @parent.last_name %></p>
      <div class="spacer"></div>
      <p><strong>Email:</strong> <%= @parent.email %></p>
      <div class="spacer"></div>
      <%= link_to(edit_parent_registration_path, class: "btn btn-primary") do %>
        <i class="fa fa-pencil">   Mettre à jour mon profil</i>
      <% end %>
      <div class="spacer"></div>
      <%= link_to(adverts_path, class: "btn btn-primary") do %>
        <i class="fa fa-pencil"> Voir mes annonces </i>
      <% end %>
    </div>
  </div>
</div>
